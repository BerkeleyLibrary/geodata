<div class="navbar-right">
    <ul class="nav navbar-nav">
        <% if signed_in? %>
        <li>
            <%= button_to t('links.logout'), destroy_user_session_path %>
        </li>
        <% else %>
        <li>
            <%= button_to t('links.login'),  '/users/auth/calnet', method: :post, class: 'btn btn-default' %>
        </li>
        <% end %>
    </ul>
</div>
